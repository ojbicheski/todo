springBoot {
	mainClass = "${project.group}.todo.TodoApplication"
	
		buildInfo {
		properties {
			artifact = "${project.name}"
			version = "${project.version}"
			group = "${project.group}"
			name = 'Todo application'
		}
	}
	
}